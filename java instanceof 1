class Parent
{
    
}
public class Child extends Parent
{
    public static void main(String args[])
    {
        Child c=new Child();
        System.out.println(c instanceof Parent);                     //   inheritance can be instanceof relation
    }
}
